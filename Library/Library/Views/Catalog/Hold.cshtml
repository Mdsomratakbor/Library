@model Library.Web.ViewModels.CheckoutViewModels.CheckoutViewModel
<div class="container">
    <div class="card-header clearfix detailHeading">
        <h2 class="text-muted">Place Hold Library Item</h2>
    </div>

    <div class="jumbotron">
        <div class="row">
            <div class="col-md-3">
                <div>
                    <p id="itemTitle">@Model.Title</p>
                    <img class="detailImage" src="@Model.ImageUrl" />
                </div>
            </div>
            <div class="com-md-9">
                <p>There are currently @Model.HoldCount holds in the queue for this item.</p>
                @using (Html.BeginForm("PlaceHold", "Catalog", FormMethod.Post))
                {
                    @Html.HiddenFor(x => x.AssetId)
                    <div>
                        Please Insert a Library Card Id.
                        @Html.TextBoxFor(x => x.LibraryCardId)
                    </div>
                    <div>
                        <button type="submit" class="btn btn-info btn-lg">Place Hold</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>